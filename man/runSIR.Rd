% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir.R
\name{runSIR}
\alias{runSIR}
\title{Run sampling importance resampling for an nlmixr2 fit}
\usage{
runSIR(
  fit,
  nSamples = c(1000, 1000, 1000, 2000, 2000),
  nResample = c(200, 400, 500, 1000, 1000),
  thetaInflation = 1,
  omegaInflation = 1,
  sigmaInflation = 1,
  capCorrelation = 0.8,
  capResampling = 1,
  recenter = TRUE,
  boxcox = TRUE,
  workers = NULL,
  directory = NULL,
  recover = TRUE,
  addIterations = FALSE,
  omegaFallback = c("wishart"),
  sigmaFallbackRse = 30,
  omegaDf = NULL,
  fitName = as.character(substitute(fit)),
  ...
)
}
\arguments{
\item{fit}{An nlmixr2 fit object with a covariance matrix.}

\item{nSamples}{Integer vector. Requested number of samples per iteration.}

\item{nResample}{Integer vector. Requested number of resamples per
iteration. Must have the same length as \code{nSamples}.}

\item{thetaInflation, omegaInflation, sigmaInflation}{Non-negative variance
multipliers used for the first iteration proposal.}

\item{capCorrelation}{Numeric in \verb{[0, 1]}. Maximum absolute proposal
correlation after covariance construction and updates.}

\item{capResampling}{Numeric greater than or equal to one. \code{1} samples
without replacement; larger values allow limited replacement.}

\item{recenter}{Logical. If \code{TRUE}, recenter the next proposal on the best
sampled vector when any sample has negative dOFV.}

\item{boxcox}{Logical. If \code{TRUE}, use Box-Cox transformed empirical
covariances for non-final iterations.}

\item{workers}{\code{NULL}, \code{"auto"}, \code{1}, or a positive integer. Controls
parallel OFV evaluation through \code{future}. \code{NULL} leaves the current
\code{future::plan()} unchanged, \code{1} forces sequential execution, a positive
integer temporarily uses a multisession plan, and \code{"auto"} uses
\code{future::availableCores(omit = 1L)}. SIR iterations remain sequential
because each iteration updates the proposal for the next iteration.}

\item{directory}{Output directory. If \code{NULL}, a numbered
\verb{<fitName>_sir_<N>} directory is created.}

\item{recover}{Logical. If \code{TRUE} and \code{directory} contains \code{sir_state.rds},
resume from the last completed iteration when possible.}

\item{addIterations}{Logical. If \code{TRUE}, append the supplied \code{nSamples} and
\code{nResample} schedule after an existing completed state in \code{directory}.}

\item{omegaFallback}{Method for omega fallback uncertainty. Currently only
\code{"wishart"}.}

\item{sigmaFallbackRse}{Percent relative standard error for sigma-like
fallback uncertainty when no SE is available.}

\item{omegaDf}{Optional degrees of freedom for the omega Wishart-style
fallback; defaults to \code{nsub - 1}.}

\item{fitName}{Name used when creating an automatic output directory.}

\item{...}{Reserved for future PsN-compatible inputs.}
}
\value{
A data frame of final SIR summary statistics with class
\code{c("nlmixr2SIR", "data.frame")}. Attributes include
\code{iterationSummary}, \code{iterations}, \code{resampledMat}, \code{covMatrix},
\code{corMatrix}, and \code{outputDir}.
}
\description{
\code{runSIR()} runs one or more sampling importance resampling iterations using
an nlmixr2 fit as the reference model. It follows the PsN SIR workflow where
practical for nlmixr2: sample parameter vectors from a proposal covariance,
evaluate them with population parameters fixed, compute importance weights,
resample, and use the empirical resampled covariance as the next proposal.
}
\examples{
\dontrun{
sir <- runSIR(
  fit,
  nSamples = c(1000, 1000, 1000),
  nResample = c(200, 400, 500),
  workers = 4
)
}
}
