% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistart.R
\name{multistartControl}
\alias{multistartControl}
\title{Control options for \code{multistart()}}
\usage{
multistartControl(
  n = 10L,
  nFit = NULL,
  sampling = c("uniform", "lhs", "normal"),
  spread = 0.2,
  which = NULL,
  perturbOmega = TRUE,
  omegaFold = 2,
  around = c("final", "initial"),
  screen = c("posthoc", "none"),
  refitBest = TRUE,
  excludeBoundary = TRUE,
  keepFits = TRUE,
  seed = 1234L,
  cores = 1L,
  cacheDir = NULL,
  restart = FALSE
)
}
\arguments{
\item{n}{Number of candidate starting points to generate.  The first
candidate is always the unperturbed starting point, so \code{n = 1} reproduces
the original fit.}

\item{nFit}{Number of candidates to fully estimate after screening.  \code{NULL}
(the default) estimates every candidate.  Ignored when \code{screen = "none"}.}

\item{sampling}{How the starting points are drawn around the initial
estimates: \code{"uniform"} (the default) draws uniformly within \code{spread}, \code{"lhs"}
uses a Latin hypercube over the same interval so the range is covered more
evenly, and \code{"normal"} draws normally with a standard deviation of \code{spread}.}

\item{spread}{Fractional spread of the perturbation on the estimation scale.
A parameter with initial estimate \code{est} is perturbed within
\verb{est +/- spread*max(abs(est), 1)}.}

\item{which}{Names of the population parameters to perturb; \code{NULL} (the
default) perturbs every unfixed population parameter.}

\item{perturbOmega}{Should the between-subject variability estimates be
perturbed as well?}

\item{omegaFold}{Fold-range for the \code{omega} perturbation.  A variance \code{v} is
drawn within \code{v/omegaFold} and \code{v*omegaFold}.}

\item{around}{When starting from a fit, perturb around the fit's \code{"final"}
estimates (the default, which asks "is this a local optimum?") or around
the \code{"initial"} estimates the fit started from (which asks "how sensitive
was this fit to where I started?").}

\item{screen}{Cheap pre-selection of candidates.  \code{"posthoc"} (the default)
evaluates the objective function at each candidate with an empirical Bayes
step only and fully estimates the best \code{nFit}; \code{"none"} estimates every
candidate.}

\item{refitBest}{Re-run the best start with the full estimation control, so
the returned fit has the covariance step and tables the exploratory runs
skip.}

\item{excludeBoundary}{Should fits with a parameter at a boundary be excluded
when picking the best start?  Matches \code{\link[=getMinAICFit]{getMinAICFit()}}.}

\item{keepFits}{Keep each start's fit in the result.  Setting this to \code{FALSE}
keeps only the summary and the best fit, which is much smaller.}

\item{seed}{Integer seed.  The perturbations are drawn from this seed and
each start is estimated with its own derived seed.}

\item{cores}{Number of starts to estimate at once.  Each estimation already
uses every available thread internally, so the default of \code{1} is usually
the fastest choice; see the "Parallel estimation" section of
\code{\link[=multistart]{multistart()}}.}

\item{cacheDir}{Directory used to cache the individual starts so that an
interrupted run can be resumed.  \code{NULL} (the default) derives a name from
the model; \code{NA} disables caching.}

\item{restart}{Discard any cached results and start over.}
}
\value{
A validated list of control options for \code{\link[=multistart]{multistart()}}
}
\description{
Control options for \code{multistart()}
}
\seealso{
\code{\link[=multistart]{multistart()}}

Other Multistart:
\code{\link[=multistart]{multistart()}},
\code{\link[=plot.nlmixr2Multistart]{plot.nlmixr2Multistart()}}
}
\concept{Multistart}
